using UnityEngine;
using System.Collections;

public class SingletonAttachPoint : MonoBehaviour
{
	// This class only exists so coroutines have a monobehaviour to latch onto
	// when called in static contexts.
}
